---
name: user-profile
description: 用户背景——力学专业学生，C 语言基础，Python 初学者，目标是 AI 大模型实习
metadata: 
  node_type: memory
  type: user
  originSessionId: bdaeac48-9351-47e8-8741-cac3a9827ec0
---

用户是力学专业学生，会一点 C 语言，Python 初学者，主要在 VS Code 里学习写代码。

目标：找 AI 大模型相关实习。
学习路线方向：Python 基础 → 数据处理 → 大模型应用 → AI + 力学/CAE 小项目。

已掌握的 Python 要点：
- 知道 Python 不能写 `count++`，要写 `count += 1`。
- 知道 `input()` 得到的是字符串，当数字用要 `int()` 或 `float()` 转换。
- 知道 `print` 放在 `for` 缩进里会循环输出，放外面只输出最终结果。
- 知道 Python 的 `print` 和 C 的 `printf` 不同：可以 `print("文字", 变量)`，也可以用 f-string。

教学相关详见 [[teaching-style]]，进度详见 [[learning-roadmap]]，环境细节详见 [[vscode-environment]]。
